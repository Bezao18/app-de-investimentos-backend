name: App-de-investimentos CI
env:
  DB_PASSWORD=7gFFNrKuWA
  DB_HOST=sql10.freesqldatabase.com
  DATABASE=sql10508355
  DB_PORT=3306
  DB_USERNAME=sql10508355	
  JWT_SECRET=segredo
  NODE_ENV=test

on:
  [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - name: Instala o node com a versão 14
        uses: actions/setup-node@v1
        with:
          node-version: 14

      - name: instalando as dependências
        run: npm install

      - name: realizando os testes
        run: npm test